{
  "name": "@endfield/web",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@endfield/data": "0.1.0",
    "@endfield/domain": "0.1.0",
    "@endfield/optimizer": "0.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "scripts": {
    "build": "npm run build --workspace @endfield/domain && npm run build --workspace @endfield/data && tsx ../../scripts/clean-dist.ts dist && tsx scripts/sync-static.ts && vite build",
    "dev": "npm run build --workspace @endfield/domain && npm run build --workspace @endfield/data && tsx scripts/sync-static.ts && vite",
    "preview": "vite preview"
  }
}
